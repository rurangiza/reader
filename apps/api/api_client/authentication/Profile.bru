meta {
  name: Profile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/auth/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{active_jwt_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
